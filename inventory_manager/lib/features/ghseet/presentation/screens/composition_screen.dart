import 'package:flutter/material.dart';

class CompositionScreen extends StatelessWidget {
  const CompositionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("Compositions")]);
  }
}
